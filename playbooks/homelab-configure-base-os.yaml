---
- name: Configure base hosts os

  hosts: all

  vars:

    base_os_packages:
      - rsync
      - python3-passlib

  tasks:

    - name: Update and upgrade hosts using apt
      ansible.builtin.include_tasks:
        file: tasks/configure-base-os.yaml
