DOMAIN=https://{{ vaultwarden_fqdn | default('dummy') }}
LOGIN_RATELIMIT_MAX_BURST=10
LOGIN_RATELIMIT_SECONDS=60
ADMIN_RATELIMIT_MAX_BURST=10
ADMIN_RATELIMIT_SECONDS=60
ADMIN_TOKEN={{ vaultwarden.admin_token | default('dummy') }}
SENDS_ALLOWED=true
EMERGENCY_ACCESS_ALLOWED=true
WEB_VAULT_ENABLED=true
SIGNUPS_ALLOWED=false
SIGNUPS_VERIFY=false
SIGNUPS_VERIFY_RESEND_TIME=3600
SIGNUPS_VERIFY_RESEND_LIMIT=5
SIGNUPS_DOMAINS_WHITELIST={{ vaultwarden_fqdn | default('dummy') }}
SMTP_HOST={{ smtp.host }}
SMTP_FROM={{ smtp.username }}
SMTP_FROM_NAME=Vaultwarden
SMTP_SECURITY=starttls
SMTP_PORT={{ smtp.port }}
SMTP_USERNAME={{ smtp.username | default('dummy') }}
SMTP_PASSWORD={{ smtp.password | default('dummy') }}
SMTP_AUTH_MECHANISM="Plain"
